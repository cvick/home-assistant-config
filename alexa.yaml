smart_home:
    endpoint: https://api.amazonalexa.com/v3/events
    client_id: !secret alexaClientID
    client_secret: !secret alexaClientSecret
    filter:
      exclude_entity_globs:
        - media_player.plex*
        - automation.*